const puppeteer = require('puppeteer');
const fs = require("fs");
const { navigateFromOfferToCompanyWebSite, colorLog, scrapJobDetailsFromPage, scrapUntilDateReach } = require('./utils');
const WebLink = require('../Models/WebLink');

const colors = require('../colorsLog');

(async () => {

})()